export const workbenchTools = [
  { name: 'arcorbit_scene_read', description: 'Read this work item, accepted scene facts, references, current execution and revision. Claims are distinct from verified outcomes.', inputSchema: { type: 'object', properties: {}, additionalProperties: false } },
  { name: 'arcorbit_scene_update', description: 'Publish a current-work report or propose agreements/criteria for this work item. Use the current revision and a stable request_id. Does not mark work complete or accept results.', inputSchema: { type: 'object', properties: { action: { type: 'string', enum: ['report', 'agreement.propose', 'criteria.propose'] }, expected_revision: { type: 'integer' }, request_id: { type: 'string' }, input: { type: 'object', properties: { text: { type: 'string' }, summary: { type: 'string' }, current: { type: 'string' }, advances: { type: 'array', items: { type: 'string' } }, remaining: { type: 'array', items: { type: 'string' } }, next: { type: 'string' }, plan: { type: 'array', items: { type: 'string' } }, artifacts: { type: 'array', items: { type: 'object', properties: { path: { type: 'string' }, summary: { type: 'string' }, version: { type: 'string' } }, required: ['path','summary'], additionalProperties: false } } }, additionalProperties: false } }, required: ['action','expected_revision','request_id','input'], additionalProperties: false } },
  { name: 'arcorbit_capabilities', description: 'Discover the software operations available in this work item and their input contracts.', inputSchema: { type: 'object', properties: {}, additionalProperties: false } },
  { name: 'arcorbit_call', description: 'Call a discovered capability in this work item. Same business validation and permissions as the UI. It cannot grant Auto or accept results.', inputSchema: { type: 'object', properties: { action: { type: 'string' }, expected_revision: { type: 'integer' }, request_id: { type: 'string' }, input: { type: 'object' } }, required: ['action','request_id','input'], additionalProperties: false } }
];

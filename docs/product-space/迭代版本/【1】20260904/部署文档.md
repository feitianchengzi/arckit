# 客户支持通道 — 生产部署文档

> 本文档指导"客户支持通道"迭代投产到生产环境。每一步标注**做什么**、**怎么做**、**验收标准**。代码已 100% 完成(见 `迭代总结.md`、`implementation-handoff.md`),部署只涉及基础设施与配置上线。

## 0. 前置依赖清单

生产环境必须先具备以下基础设施。**缺一项链路可能降级但不一定断裂**——三个外部依赖均有降级策略,但为完整能力建议全部就绪。

| 依赖 | 用途 | 缺失影响 | 降级策略 |
|---|---|---|---|
| PostgreSQL 12+ | 反馈/消息/通知/分诊/Agent 会话持久化 | 服务无法启动 | 无降级,必须就绪 |
| API 网关 | 统一认证(JWT/APIKey)、注入 X-User-ID、签发/校验 feedback session token、转发请求 | SDK 无法鉴权 | 无降级,必须就绪 |
| 阿里云 OSS/STS | 反馈附件上传与访问 | 附件功能不可用 | 文本反馈仍可用,附件报错 |
| OpenHands Agent Server | 智能客服基于代码仓库的问答 | 智能客服转人工兜底 | Agent 不可用时智能客服降级提示,不阻断反馈提交 |
| Embedding BGE-M3 服务 | 代码语义检索(RAG) | 代码语义检索不可用 | 退化为关键词检索,Agent 仍可直读代码 |
| LLM API(经 OpenHands 调用) | Agent 推理 | Agent 无法生成回复 | 智能客服降级提示 |
| CORS/Origin 白名单 | SDK iframe 跨域 | SDK 无法 postMessage | 必须配置含 SDK 域名 |
| 网关 WS 透传 | V2 WebSocket 实时推送 | 无实时推送,SDK 退化为轮询 | 必须支持 WS Upgrade + Sec-WebSocket-Protocol 透传 |

### 0.1 网关专项要求(本轮新增 V2 WS)

- **session token 签发**:网关或宿主后端用 API Key 调 `POST /workshop/v2/apikey/feedback-sessions` 换取 15 分钟 `fbs_` token 下发客户端。
- **session scope 注入**:网关校验 `fbs_` token 后注入 `X-Auth-Type: feedback_session` + `X-Feedback-Gateway-Secret`(与 workshop `FEEDBACK_GATEWAY_SHARED_SECRET` 一致)+ `X-Feedback-Project-ID` / `X-Feedback-Custom-User-ID` / `X-Feedback-Session-ID`。
- **WS 透传**:V2 SDK 通过 `wss://<gateway>/workshop/v2/feedback/projects/:id/ws` 连接,token 走 `Sec-WebSocket-Protocol: nebula-auth.<token>`。网关必须**透传 subprotocol 字段**并支持 WS Upgrade,不能剥离 `Sec-WebSocket-Protocol`。
- **`FEEDBACK_SESSION_SIGNING_KEY` 与 `FEEDBACK_GATEWAY_SHARED_SECRET`**:网关与 workshop-api 必须使用相同值,且两者是**不同**的密钥(见 `env.workshop.production.example:73-76`),用 `openssl rand -base64 48` 生成,≥32 字节,不提交。

---

## 1. 后端 workshop-api 部署

### 步骤 1.1 准备环境变量
**做什么**:基于示例生成生产 env。
**怎么做**:
```bash
cd services/workshop-api/deploy/prod
cp env.workshop.production.example .env.workshop.production
# 编辑 .env.workshop.production,填写实际值:
# - DB_* :RDS 连接
# - CORS_ALLOW_ORIGINS :生产域名(含 SDK 域名、Console 域名)
# - OSS_* :阿里云 OSS AccessKey/Secret/Region/Bucket/RAM Role ARN/RootPath
# - FEEDBACK_SESSION_SIGNING_KEY :openssl rand -base64 48
# - FEEDBACK_GATEWAY_SHARED_SECRET :与网关一致,另生成一个
# - FEEDBACK_NOTIFICATION_SHARED_SECRET / NOTIFICATION_INTERNAL_SHARED_SECRET
# - LLM_API_KEY / LLM_BASE_URL / LLM_MODEL(给 OpenHands)
```
**验收标准**:`.env.workshop.production` 中所有 `REPLACE_WITH_*` 已替换为实际值;密钥均 ≥32 字节且未提交 Git(`git status` 不显示该文件)。

### 步骤 1.2 构建并启动 workshop-api
**做什么**:构建镜像、启动服务。
**怎么做**:
```bash
cd services/workshop-api/deploy/prod
docker compose -f docker-compose.prod.yml build
docker compose -f docker-compose.prod.yml up -d
```
**验收标准**:
- `docker compose -f docker-compose.prod.yml ps` 中 todo-service 状态 `Up`。
- `curl https://<gateway>/workshop/v1/public/health` 返回 200。
- 容器日志无 `FEEDBACK_SESSION_SIGNING_KEY 未配置` 等报错。

### 步骤 1.3 执行数据库迁移
**做什么**:应用本轮及之前的 schema 变更(12+ SQL 迁移,含 openhands_agents、customer_code_repos、agent_conversations、feedback_messages 扩展字段、code_chunks 等)。
**怎么做**:
```bash
# 生产发布由 remote-deploy.sh 在切换流量前显式执行(DB_AUTO_MIGRATE=false)
./todo migrate
# 或按 README-openhands.md 逐个执行迁移:
# psql -h <rds> -U workshop -d workshop -f database/migrations/20260917_openhands_config_up.sql
```
**验收标准**:
- 迁移无报错退出。
- `\dt` 存在 `feedbacks`、`feedback_messages`、`feedback_notifications`、`openhands_agents`、`customer_code_repos`、`agent_conversations`、`code_chunks` 等表。
- `feedback_messages` 含 `conversation_id`、`tool_calls`、`confidence`、`sender_custom_user_id` 字段。

### 步骤 1.4 部署 OpenHands Agent Server(智能客服依赖)
**做什么**:启动 OpenHands Agent + workspace 容器。
**怎么做**:
```bash
cd services/workshop-api/deploy/dev
# 在 .env.development 或生产等价配置中设置:
# LLM_API_KEY / LLM_BASE_URL / LLM_MODEL / OH_SESSION_API_KEYS_0 / OPENHANDS_PORT
# 智能客服多模态（看图）：切换到支持 vision 的 LLM 模型后，置 AGENT_VISION_ENABLED=true，
# 客户在对话窗口上传的图片会以 image_url 传给 Agent 真实分析。
# AGENT_VISION_ENABLED 默认 false（纯文本模型不传图，避免 OpenHands 侧报错）。
docker compose -f docker-compose.dev.yml -f docker-compose.openhands.yml up -d openhands-agent openhands-workspace
```
**验收标准**:
- `curl http://<openhands-host>:8000/health` 返回 200。
- workshop-api 日志中 `CheckOpenHandsHealthHandler`(`GET /workshop/v2/user/projects/:id/openhands-health`)返回 healthy。
- 客户代码仓库按项目只读挂载到 OpenHands workspace,容器间网络隔离。
- 多模态:置 `AGENT_VISION_ENABLED=true` + `LLM_MODEL=<vision 模型>` 后,客户上传图片,Agent 回复能引用图片内容。

### 步骤 1.5 配置客户项目 OpenHands 与知识源
**做什么**:为每个客户项目配置 OpenHands agent 与代码仓库。
**怎么做**:通过 Console 或 API:
```
PUT /workshop/v2/user/projects/:id/openhands-config   # Agent 配置
POST /workshop/v2/user/projects/:id/code-repos        # 绑定客户代码仓库
POST /workshop/v2/user/projects/:id/code-repos/:repoId/index  # 触发代码索引
```
**验收标准**:
- `GET /workshop/v2/user/projects/:id/openhands-health` 返回 healthy。
- `GET /workshop/v2/user/projects/:id/knowledge/code-stats` 显示代码 chunk 已索引。
- `POST /workshop/v2/user/projects/:id/knowledge/retrieve-test` 能检索到代码片段。

---

## 2. SDK 构建与部署

### 步骤 2.1 构建 V2 SDK 产物
**做什么**:构建 SDK SPA,注入网关地址与允许的宿主 origin。
**怎么做**:
```bash
cd packages/feedback-sdk-web
# 注入运行时默认值(仍可被 configure 覆盖)
VITE_GATEWAY_URL=https://<gateway> \
VITE_PUBLIC_BASE=/sdk-v2 \
VITE_SDK_PARENT_ORIGINS=https://<your-app>,https://<console> \
npm run build:v2:check   # tsc + vite build,产物在 dist/
```
**验收标准**:
- `tsc` 无类型错误。
- `dist/index.html` 与 `dist/assets/*` 生成。
- 产物中不含硬编码 `api.feitianchengzi.com`(本轮已移除该 fallback)。

### 步骤 2.2 上传到 OSS
**做什么**:将 V2 产物上传到 OSS 的 `/sdk-v2/` 前缀。
**怎么做**:
```bash
cd packages/feedback-sdk-web
# 配置 OSS 凭证后执行
npm run deploy:oss:v2   # 调 scripts/deploy-feedback-sdk-v2.sh
```
**验收标准**:
- `https://<sdk-host>/sdk-v2/index.html` 可访问,返回 SDK HTML。
- 该脚本仅写 `/sdk-v2/`,不影响 `/sdk/`(V1)。

---

## 3. ArcOrbit Desktop 部署

### 步骤 3.1 更新 renderer 产物
**做什么**:打包含本轮 renderer 改动的 Desktop 版本(反馈会话自动滚动、实时刷新、新消息 toast、V2 WS 客户端)。
**怎么做**:按 ArcOrbit 既有发版流程构建并分发 Desktop 安装包。
**验收标准**:
- 客服在反馈详情页回复 → 消息列表自动滚到底部显示新消息。
- 收到非当前反馈的新消息 → 顶部 toast 提示 + 未读 badge 更新。
- 单测通过:`node runtime/arcorbit/scripts/run-tests.mjs`(880+ 测试,含本轮新增的 `feedback 实时刷新`、`scrollFeedbackConversationToBottom` 用例)。

---

## 4. 网关与域名配置

### 步骤 4.1 路由与 CORS
**做什么**:网关配置 V2 路由转发、CORS、WS 透传。
**验收标准**:
- `POST /workshop/v2/apikey/feedback-sessions` 可返回 `fbs_` token。
- `GET /workshop/v2/feedback/projects/0/ws`(带 `Sec-WebSocket-Protocol: nebula-auth.<fbs_token>`)升级成功,收到 `system.connected`。
- 非法 token 连 WS 返回 401 不升级。
- CORS `Access-Control-Allow-Origin` 包含 SDK 域名与 Console 域名。

### 步骤 4.2 Console 自反馈项目(可选)
**做什么**:若启用 Console 自反馈,配置 `FEEDBACK_CONSOLE_PROJECT_ID`。
**验收标准**:Console 用户可对自己项目反馈,身份由登录用户 ID 派生(`console_<userID>`),无需项目成员校验。

---

## 5. 端到端验收(投产后)

按以下顺序逐项验证,全部通过方可宣告投产完成。

### 5.1 SDK 集成与身份
**做什么**:宿主前端/移动端用 iframe/WebView 嵌入 SDK,session 模式注入 `fbs_` token。
**验收标准**:
- 客户提交反馈 → 生成 Feedback,编号回显。
- 同一 `project_id` 不同 `custom_user_id` 的反馈互相隔离(客户 A 看不到客户 B 的反馈)。
- session token 过期后 SDK 触发 `feedback-sdk:refresh-session`,宿主刷新后恢复。

### 5.2 智能客服
**做什么**:客户在对话窗口提问。
**验收标准**:
- Agent 基于代码仓库回复,显示检索的工具(代码仓库/项目文档/产品知识库)与置信度。
- session 模式下 Agent 对话可用(本轮修复的 bug)。
- **对话窗口无转人工入口**(本轮移除)。
- **对话窗口支持上传图片**(选择/预览/发送);`AGENT_VISION_ENABLED=true` 且多模态模型时 Agent 能看图回复。
- Agent 不可用时降级提示,不阻断反馈提交。

### 5.3 客服回复→客户面板消息
**做什么**:客服在 Console/Desktop 反馈详情页回复。
**验收标准**:
- 客户在对话窗口时:新消息向上推显示(自动滚动到底),无需手动刷新。
- 客户不在对话窗口时:SDK 展示未读红点(`feedback-sdk:unread-changed`);Desktop 客服侧非当前反馈 toast + badge。
- 外部 iframe 宿主收到 `feedback-sdk:unread-changed` 并展示红点。

### 5.4 实时通道
**验收标准**:
- SDK 连接 V2 WS 后 `已连接实时推送` 提示出现(`SDKStatusPage` 的 connected 状态)。
- 客服回复后 1s 内客户端收到新消息(WS 推送),而非等 30s 轮询。

### 5.5 全链路
**验收标准**(对应 PRD US-01~US-09):
- 客户对话反馈→确认提交生成 Feedback(US-01)。
- 我的反馈显示状态时间线(US-02)。
- 内部分诊门控可转 Task 或忽略(US-04)。
- Task 认领进 Codex loop(US-05)。
- 进展草稿确认后发客户(US-06)。
- 验收后构建产物,artifact_url 回写,客户可见(US-07)。

### 5.6 i18n(本轮新增)
**验收标准**:`window.FeedbackSDK.setLocale('en-US')` 后 SDK 文案切换为英文;`configure({ locale: 'zh-CN' })` 切回中文。

---

## 6. 回滚预案

- **后端**:`docker compose -f docker-compose.prod.yml down` + 切回上一镜像;迁移若已执行需评估回滚迁移(逐个 down 脚本)。
- **SDK**:OSS `/sdk-v2/` 覆盖回上一版本产物(保留历史版本便于回滚)。
- **Desktop**:分发上一安装包版本。
- **网关 V2 WS**:若 WS 透传有问题,SDK 自动退化为 30s 轮询,链路不断(功能降级而非中断)。

## 7. 验证命令速查

```bash
# 后端测试(投产前)
cd services/workshop-api && go test ./...

# SDK 测试 + 构建
cd packages/feedback-sdk-web && npx vitest run && npx tsc --noEmit && VITE_GATEWAY_URL=https://<gateway> npm run build:v2:check

# ArcOrbit 测试
node runtime/arcorbit/scripts/run-tests.mjs

# V2 WS 连通性(带 token)
# wscat -c "wss://<gateway>/workshop/v2/feedback/projects/0/ws" -s "nebula-auth.<fbs_token>"

# 健康检查
curl https://<gateway>/workshop/v1/public/health
curl http://<openhands>:8000/health
```

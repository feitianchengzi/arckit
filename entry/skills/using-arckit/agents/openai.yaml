interface:
  display_name: "Using Arckit"
  short_description: "用同一 Agent 按 Case gap 持续推进软件开发"
  default_prompt: "使用 $using-arckit 识别原任务中的相关缺口，选择当前最关键、最值得独立解决的一项；新事实服务当前缺口时继续使用，其他独立缺口记录后重新选择。依据证据判断原任务进展与是否续轮，区分必要准备和重复工作；明确下一责任，遵守用户停止。"
policy:
  allow_implicit_invocation: true

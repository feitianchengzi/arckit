# Arckit Skill Repository Project State

Status: active
Updated: 2026-07-08T04:21:48.000Z
Canonical Record: state.record.json

## Purpose

治理 Arckit skill 体系，使其同时支撑 Codex 类人机协作和多 Agent 自动化平台的软件开发接力协议

## Loop Focus

实现 Arckit Runtime supervised agent loop。

## Next State Transition

- Transition: quality_validation: verified -> accepted
- Why now: 真实 supervised turn 已证明 event stream、/steer 和 /interrupt 控制路径；Desktop Client 已提供项目列表、连续 Chat、状态检查器、事件观察和控制入口，但还未通过桌面端产出 round_result=done。
- Next action: 在可联网环境中通过 Desktop Chat 重跑 supervised turn，获得 done result 后通过 gate/write-ledger。
- Next responsibility: agent
- Trigger mode: manual_bridge
- Agent continuation available: yes
- Human decision required: no
- Continuation prompt: 继续 active case CASE-20260707-002：使用 runtime/arckit-runtime Electron Desktop Chat 添加一个真实项目，发送任务消息发起 codex-app-server supervised turn，运行中发送 Chat 消息测试 steer，观察右侧状态和 events，保存 result/events，确认 round_result=done 后运行 gate-result 和 write-ledger。

## Current Risks

- GAP-runtime-real-supervised-turn: 真实 supervised turn 已证明 event stream、/steer 和 /interrupt 控制路径；Desktop Client 已提供项目列表、连续 Chat、状态检查器、事件观察和控制入口，但还未通过桌面端产出 round_result=done。 Risk=high Urgency=critical
- GAP-cross-record-audit: project、iteration 和 case 之间如果缺少一致性审计，长期 loop 可能出现引用漂移或状态不一致。 Risk=medium Urgency=medium

## Do Not Treat As Complete

- Stop condition: 当前迭代的 stop condition 是完成至少一次 Runtime-supervised agent loop，并根据结果决定是否补跨记录 audit。
- Do not edit this file as source state; update `state.record.json` and render this projection.
- Do not close the active iteration until its close condition is met.

## High-Priority Dimensions

- problem_scenarios: defined -> accepted; next: 选择一个复杂软件项目作为状态驱动 loop 验证对象。
- product_behavior: designed -> accepted; next: 将状态机制用于下一轮真实开发任务，观察 loop_control 和 state_gaps 是否能正确驱动行动。
- user_experience: implemented -> accepted; next: 用 Electron Desktop Chat 对一个真实项目启动 Codex app-server run，运行中用 Chat steer，观察右侧状态并执行 gate/write-ledger。
- architecture_foundation: implemented -> accepted; next: 为 codex-app-server adapter 增加 turn timeout 和 transcript compaction 策略。
- implementation_coverage: verified -> accepted; next: 用 Desktop Chat 重跑 codex-app-server supervised turn 并获得 done result。
- quality_validation: verified -> accepted; next: 在可访问 OpenAI Responses API 的环境中用 Desktop Chat 重跑 supervised turn，保留 steer/interrupt 证据，并在 gate 允许后执行 write-ledger。

## Read For Precision

- state.record.json
- arckit/project/iterations/ITER-20260705-001-state-driven-loop-foundation.record.json
- arckit/cases/active/CASE-20260707-002-implement-arckit-runtime-mvp.md

## Open Questions

- 是否用一个真实复杂项目执行 state gap -> case -> verification -> state delta 的完整 loop 验证。
- 是否增加跨 project/iteration/case 的一致性 audit 命令。

## Notes

- `state.record.json` is the canonical machine-readable project state.
- `STATE.md` is a generated loop decision brief. It is intentionally lossy and should not mirror the full JSON record.
- Store iteration state under `arckit/project/iterations/` and case evidence under `arckit/cases/`.

# Arckit Skill Repository Project State

Status: active
Updated: 2026-07-05T16:33:20.000Z
Canonical Record: state.record.json

## Purpose

治理 Arckit skill 体系，使其同时支撑 Codex 类人机协作和多 Agent 自动化平台的软件开发接力协议

## Loop Focus

用真实复杂项目验证状态驱动 loop。

## Next State Transition

- Transition: quality_validation: verified -> accepted
- Why now: 没有真实复杂项目 loop 验证时，状态机制只能证明结构合理，不能证明能驱动工程收敛。
- Next action: 选择真实复杂项目执行一次 state gap -> case -> verification -> state delta 的闭环。

## Current Risks

- GAP-state-loop-validation: 没有真实复杂项目 loop 验证时，状态机制只能证明结构合理，不能证明能驱动工程收敛。 Risk=high Urgency=critical
- GAP-cross-record-audit: project、iteration 和 case 之间如果缺少一致性审计，长期 loop 可能出现引用漂移或状态不一致。 Risk=medium Urgency=medium

## Do Not Treat As Complete

- Stop condition: 当前迭代的 stop condition 是完成至少一次真实复杂项目 loop 验证，并根据结果决定是否补跨记录 audit。
- Do not edit this file as source state; update `state.record.json` and render this projection.
- Do not close the active iteration until its close condition is met.

## High-Priority Dimensions

- problem_scenarios: defined -> accepted; next: 选择一个复杂软件项目作为状态驱动 loop 验证对象。
- product_behavior: designed -> accepted; next: 将状态机制用于下一轮真实开发任务，观察 loop_control 和 state_gaps 是否能正确驱动行动。
- architecture_foundation: implemented -> accepted; next: 用复杂项目执行一轮从 state gap 到 case 到 validation 到 state delta 的完整闭环。
- implementation_coverage: verified -> accepted; next: 用真实复杂项目验证 state gap、iteration state 和 case evidence 是否能稳定驱动下一轮行动。
- quality_validation: verified -> accepted; next: 选择真实复杂项目执行一次 state gap -> case -> verification -> state delta 的闭环。
- maintainability_handoff: implemented -> accepted; next: 在下一次项目任务开始时，以 project state summary 作为 loop 输入。

## Read For Precision

- state.record.json
- arckit/project/iterations/ITER-20260705-001-state-driven-loop-foundation.record.json

## Open Questions

- 是否用一个真实复杂项目执行 state gap -> case -> verification -> state delta 的完整 loop 验证。
- 是否增加跨 project/iteration/case 的一致性 audit 命令。

## Notes

- `state.record.json` is the canonical machine-readable project state.
- `STATE.md` is a generated loop decision brief. It is intentionally lossy and should not mirror the full JSON record.
- Store iteration state under `arckit/project/iterations/` and case evidence under `arckit/cases/`.

# Idea 环境探索职责优化

用户批准：Agent 负责理解、环境探索、诊断和计划；ArcOrbit 提供能力和事实，负责授权、人工确认、精确执行、回执与存储，并沉淀产品规范。

Mole ScreenRecorder 接入中，工具丢失底层错误、把 GitHub 未确认一律引导登录；终端发现 /usr/local/bin/gh 可用，不能据此认定业务主进程已正常。前轮隔离复现证明 ENOENT 被吞，真实 Mole 的 Git 故障尚无运行证据。

保持 Idea 四条存储规则、共享 Chat、既有生命周期、无新增服务端、无产品迭代或发布范围。改进应让 Agent 在快捷检查失败后继续使用原生能力探索，不能把语义排查变成固定步骤程序。明确原生命令与业务工具的执行证据及权限边界，不以 prompt 冒充 OS 沙箱。

# Gap、Loop 与软件不变量维护记录（2026-09-16）

本文件记录一次维护与验证交接，不随技能作为执行协议分发。代表性场景见 [隔离评估场景](../../../../quality/evaluations/state-driven-loop/scenarios.md)。

## 本次维护边界

正式源和工作副本均为当前 Arckit 仓库的 `entry/skills/using-arckit/` 与 `entry/skills/arckit-development-ledger/`。只维护方法、模板、通用契约及相关测试；其他 Project State 定义不在本期。模板加载不作语义分类，Runtime 不新增选题策略。现有项目通过 trusted reconciliation 更新定义并保留历史含义与未完成义务；这不代表安装副本已经升级。

## post_maintenance_handoff

```yaml
recommended_next_step: verify_with_skill_first
reason: 方法和模板改变真实执行判断；确定性回归与结构检查不能替代新 Agent 隔离执行观察。
formal_source_path: entry/skills/{using-arckit,arckit-development-ledger}
working_copy_path: entry/skills/{using-arckit,arckit-development-ledger}
maintenance_source_path: entry/skills/{using-arckit,arckit-development-ledger}
validation_required: true
governance_required: false
arcforge_action_hint: none
user_confirmation_required: false
```

可选隔离验证使用所链接的隔离评估场景，在 `mktemp -d` 创建的独立工作区运行目标 skills。只允许修改隔离 repo 和证据目录，不写真实项目、安装副本或外部系统。已知限制：本轮不声明完成 fresh-agent 隔离验证；安装、同步或发布另按用户明确授权范围处理。

## 本地验证记录（2026-09-16）

- `node --test --test-reporter=dot runtime/arcorbit/test/{project-ledger-governance,case-transition,coherent-agent-loop,ledger-writer,semantic-case-command,protocol-compatibility,state-condition-case}.test.mjs`：54 项通过。
- 新增 semantic 重新界定集成场景后，`node --test --test-reporter=spec runtime/arcorbit/test/{semantic-case-command,package-distribution,skill-provisioning-manager,capability-policy}.test.mjs`：30 项通过；与上一组存在重叠，不相加作为不同用例数。
- 覆盖替换模板名称/数量/顺序后的动态加载和精确校验、动态 assessment 覆盖、取消旧边界后保留开放 Gap、semantic apply 后 fresh-read、模板随分发包携带及安装。
- 两个目标 skill 的 12 个 JSON 可解析，23 个本地 Markdown 引用有效，frontmatter 和 agents YAML 可解析；脚本语法与 `git diff --check` 通过。
- 仓库 Project State 经 trusted reconciliation dry-run 和正式 apply 后为 compatible，fresh snapshot available；仅同步不变量集合及 Project revision/timestamp，其他软件定义与 advancement 保持逐值一致。旧集合中不再属于核心模板但仍被引用的定义保留为兼容项；active/closed Case 历史未改写。
- 尚未执行上述场景的 fresh-agent 隔离观察；本记录不证明 Agent 行为质量已经通过真实执行验收。

## 内容归位

本记录由原 `entry/skills/using-arckit/references/maintenance-validation.md` 迁出；评估场景独立保存，业务 SKILL.md 不再引导执行 skill 维护流程。本次不变量字段结构保持原 schema；这属于本次变更事实，不作为后续版本的固定契约。历史保真与恢复规则集中在 Ledger 的 protocol-reconciliation reference。

## 连带优化收口

- 按用户确认撤回 debug skill 的主文件、入口元数据与方法论修改，以及 Definition 共享契约修改；`git diff --exit-code` 确认这些文件与本轮修改前一致。不改变专业能力的原有方法。
- Loop 通用契约明确专业能力不自动扩张当前 Gap，诊断与修复是否同轮取决于当前独立问题，不按阶段强拆；Ledger 通用契约区分专业能力结果与动态不变量判断。
- ArcOrbit 主执行上下文改为源文件引用、可信入口与必要恢复信息，不再转储 Project/Case 状态。prompt 使用工作区相对路径引导读取；Workbench 通过 scene 工具按需读取，不将 task/scene JSON 嵌入 prompt，场景报告与 Ledger 提交保持分离。
- 已移除无生产消费者的 controller context digest 及仅针对它的测试。其历史对象字符串化和不变量截短问题随重复摘要链一起消除；不新增第二种历史或规则解释机制。
- 本次修改仍以正式维护源为工作区，能力承载为方法契约、已有 Runtime 和外部回归测试；无新专业 skill、安装同步、提交或发布。

本次回归：`node --test --test-reporter=dot runtime/arcorbit/test/{coherent-agent-loop,state-condition-case,protocol-compatibility,project-workbench,package-distribution,skill-provisioning-manager,capability-policy,project-ledger-governance,case-transition,semantic-case-command}.test.mjs`：87 项通过。两个入口 skill 的 12 个 JSON、23 个本地 Markdown 引用、frontmatter 与入口 YAML 检查通过；修改脚本语法与 `git diff --check` 通过。

`post_maintenance_handoff` 沿用上文：`verify_with_skill_first`，正式源、工作副本与维护源均为本仓库两个 entry skills；无需治理写入。后续隔离任务使用外部场景文件，观察 Gap 边界、动态读取与事实源恢复。当前仅完成本地结构和确定性验证，不宣称真实 Agent 行为验收通过。
